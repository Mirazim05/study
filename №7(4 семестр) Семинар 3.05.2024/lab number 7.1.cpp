#include <iostream>
#include <vector>
#include <list>

using namespace std;

int main() {
    // ??????? ??????
    vector<int> vector;
    for (int i = 0; i < 10000; i++) {
        vector.push_back(i);
    }

    // ??????? ??????
    list<int> lst;
    for (int i = 0; i < 10000; i++) {
        lst.push_back(i);
    }

    // ???????? ????? ??????, ?????????? ???????? ? ???????
    size_t vector_size = vector.size() * sizeof(int);
    size_t list_size = lst.size() * sizeof(int);

    // ??????? ??????????
    cout << "????? ??????, ?????????? ????????: " << vector_size << " ????" << endl;
    cout << "????? ??????, ?????????? ???????: " << list_size << " ????" << endl;

    return 0;
}
